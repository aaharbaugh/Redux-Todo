const initialState = {
    todos: [
        {
            id: 11, 
            value: 'This is the first Todo',
            completed: false,
        },
    ]
};

function reducer(state = initialState, action){
    switch(action.type){
        case: ADD_TODO:
            return 
    }
}

export default reducer;